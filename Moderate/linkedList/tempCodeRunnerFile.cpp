void reverseRecursion(Node* &head,Node* prev,Node* &curr,Node* &forward){
//     if(curr == NULL){
//         head = prev;
//         return;
//     }

//     forward = curr -> next;
//     curr -> next = prev;
//     reverseRecursion(head,curr,forward);
// }